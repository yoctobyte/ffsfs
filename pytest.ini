[pytest]
pythonpath = .
markers =
    unit: fast local unit tests
    vm: tests intended to run inside disposable VMs
    fuse: tests that mount or exercise FUSE behavior
    network: tests that use network sockets or peer communication
    two_peer: tests requiring two peer nodes
    destructive: tests that may alter mounted filesystems or runtime state
    slow: slower tests excluded from quick smoke runs
